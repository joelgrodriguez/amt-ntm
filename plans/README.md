# Implementation Plans

Generated by the improve skill on 2026-07-10 (pre-prod-launch audit, planned at
commit `91b252b`). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions. The reviewer maintains this index.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | PHP lint gate + minimal CI | P1 | S–M | — | DONE (landed 5133523) |
| 009 | Remove placeholder Schema Pro markup (db 041) | P1 | S | — | DONE (landed f8e20f9; drafting alone insufficient → 010) |
| 010 | Purge placeholder Schema Pro output cache (db 042) | P1 | S | 009 | DONE (landed; 787 rows purged + canary; count 0; JSON-LD 5/5 parse) |
| 003 | Fallback SEO: robots noindex + paged canonicals | P1 | S | — | DONE (landed 7ee845f) |
| 004 | Collapse per-render product/attachment lookups | P1 | S | — | DONE (landed 06210a1) |
| 006 | A11y commitments: tap targets, contrast, table semantics, motion backstop | P1 | M | — | DONE (landed with 008 batch) |
| 002 | Machine-schema hardening (pv cast, image guard) | P2 | S | — | DONE (landed 1675e31) |
| 007 | Conversion-path robustness (HubSpot retry, URL guards) | P2 | S | — | DONE (landed 1073404) |
| 005 | Trim non-Latin font subsets | P2 | S | — | DONE (landed a557794) |
| 008 | Repo hygiene (dead parts, README, dist config, agent docs) | P2 | S–M | — | DONE (landed 4801561) |
| 011 | Manual/profile → product cross-sell wiring | P2 | M | 004 | IN PROGRESS |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- No hard code dependencies — all nine plans touch disjoint files.
- 001 should LAND first anyway: it establishes the `lint:php` verification
  baseline the other merges are checked against.
- 008 assumes `plans/` exists (it adds it to the release strip list) and
  references `lint:php` in the README rewrite only if 001 has landed — it
  checks `package.json` at execution time either way.
- Outside any plan (owner action): verify in MyKinsta that LIVE + STAGING run
  PHP ≥ 8.1 before cutover (001 adds this gate to the runbook, but the
  dashboard check is Joel's).

## Findings considered and rejected

- **Guard machine-schema.php behind `seo_plugin_active()`**: rejected —
  verified live (2026-07-10, Yoast + Premium active) that the theme is the
  ONLY Product emitter on machine pages; a guard would delete the page's only
  Product schema. Plan 002 documents this in the file header instead.
- **"FAQPage double-emitted on machine pages"**: disproved — the faq-accordion
  part is not rendered by machine templates; each page has a single FAQPage
  emitter.
- **"Product schema usually lacks offers"**: disproved — all 10 machine data
  files set `low_price`. Only the empty-image guard survived (in 002).
- **Missing width/height on `responsive_image()` fallback `<img>`**: rejected
  for now — on prod, curated URLs rebase to local attachments and get full
  dimension attributes; the bare-`<img>` branch is a dev/staging cosmetic.
- **Hero/CTA duplication across ~30 page templates**: not a finding — thin
  config wrappers delegating to shared parts is the correct pattern.
- **`inc/*-data.php` files duplicating DB state**: intentional typed-content
  lifting; low drift risk.
- **DB script numbering gaps (001–013, 015–019, 034)**: never existed;
  034 was consciously renumbered to 014.
- **Splitting the 203KB site-wide CSS bundle**: deferred, not rejected — L
  effort, MED risk, real win; post-launch candidate.
- **YouTube click-to-load facade** (`inc/video.php` oEmbeds): deferred —
  M effort; Wistia (the dominant host) already lazy-loads.
- **Splitting the 1,182-line `machines-data.php`**: deferred post-launch —
  needs the 35-URL smoke battery around it.
- **JS bundle splitting / resize-listener throttling**: not worth doing —
  app.js is 10KB gzipped; modules early-return on absent DOM.
- **`content-none.php` / `content-search.php` as dead code**: disproved —
  loaded via two-arg `get_template_part('templates/parts/content', 'none'|'search')`.
- **npm audit advisories (vite/rollup path traversal)**: dev-server/build-time
  exposure only; `npm audit fix` at leisure, no plan needed.

## Direction findings

- **Filterable CPT archives** ("the library is half-built"): **ALREADY SHIPPED —
  verified 2026-07-10, no plan needed.** TODO.md was stale. Evidence:
  `archive.php` is a dashboard archive with scoped-catalog mode for
  profile/manual (filter sidebar via `taxonomy-filter-sidebar.php`), the LC
  dashboard branch covers video/footprint/literature, `content-search.php`
  dispatches every result to its native card (`card-profile`, `card-manual`,
  `card-product`), and the mobile filter drawer exists
  (`filter-sidebar.php:194` `<details class="filter-drawer">`, `lg:hidden` in
  `filters.css:283`). Live check: `/learning-center/profile/` and
  `/learning-center/manual/` → 200 with sidebar. Do not re-audit from TODO.md.
- **Wire the Woo↔CPT machine-tag cross-sell**: genuinely unimplemented
  (`single-manual.php` `@todo` placeholder cards confirmed) → **plan 011**.
