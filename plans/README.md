# Implementation Plans

Generated by the improve skill on 2026-07-21, from a homepage SEO/AEO audit
(content + technical + LLM readiness) cross-checked against the rendered local
page, the live production site, Google Search Console data, and a second
independent review. Planned at commit `c5d139b`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

**Repo-wide execution caveats** (repeated in each plan):
- DevKinsta serves the *main checkout*, not git worktrees — rendered-page
  (`curl https://newtech.local/`) verification from a worktree only works for
  DB-side changes (plan 001); theme changes verify statically in the worktree
  and get rendered QA post-merge.
- All DB-side changes must be captured as replayable `scripts/db/NNN-*.sh`
  migrations (see `scripts/db/README.md`) — never applied only via wp-admin.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix homepage Yoast title, meta description, OG tags (db script 053) | P1 | S | — | DONE (73eb6fc) |
| 002 | Real alt text for product cards + hero slides (incl. markup-in-alt bug) | P1 | S | — | DONE (aa1a2e7) |
| 003 | Demote mega/mobile menu panel titles from H2 | P2 | S | — | DONE (cf2629a) |
| 004 | Homepage FAQ section + FAQPage JSON-LD | P2 | M | — | DONE (6645070) |
| 005 | ItemList JSON-LD of machine lineup on front page | P3 | S | — | DONE (a8c19a6) |

Executed 2026-07-21 on branch `joelgrodriguez/homepage-seo-aeo` (off master
@c5d139b — the planned-at commit, which is master's HEAD, not dev). db 053
was applied to the local DevKinsta DB (`DRY_RUN=0`) and will replay at cutover
via `npm run db:apply`.

### Deviation log

- **002 extended to `flagships.php`**: rendered verification found a *second*
  markup-in-alt instance (the flagships hero image passed a `<br>`-bearing
  headline as its alt) beyond the hero-slide case the plan scoped. Same bug
  class, same `wp_strip_all_tags` fix; touching the extra file was required to
  meet the plan's own "zero markup-in-alt" gate. 11 empty alts remain on other
  homepage surfaces (6 testimonial avatars, 3 learning-center thumbnails, 1
  financing image, 1 Facebook tracking pixel that is correctly alt-less) —
  these were explicitly deferred by the plan and are a candidate follow-up.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies — all five are independent. 001 and 002 first because
  they fix live defects; 004/005 add new surface.
- 004 and 005 both add JSON-LD to the homepage; whichever lands second should
  re-run the "count schema blocks" checks in both plans' verification steps.

## Findings considered and rejected

(So nobody re-audits these.)

- **Schema Pro `SiteNavigationElement` junk block**: already captured as
  `scripts/db/052-disable-schema-pro-site-navigation.sh` (committed `5314e42`).
  It still renders locally only because the script hasn't been applied — run
  `npm run db:apply`. No new plan needed.
- **"Create llms.txt"** (from the second review): false premise — production
  already serves a Yoast-generated `/llms.txt`. Its quality issues (truncated
  summary line, HTML soup in product descriptions) are fixed at the source by
  plan 001 and by the ongoing product-content migration, not by a handwritten
  file that would go stale.
- **"Expand seo.php JSON-LD / harden its fallback meta"**: `app/inc/seo.php`
  no-ops while Yoast is active — it's a safety net, and Yoast Premium is
  installed. Changing dormant code is not worth the diff.
- **Make the sr-only H1 visible**: `sr-only` headings are not penalized;
  the hero's visual design intentionally leads with per-slide H2s. A visible
  H1 is a design decision, not an SEO defect — revisit only as part of a hero
  redesign.
- **"Machine specs hidden in JS tabs"** (second review): false — all tab
  panels are server-rendered static HTML; extractors see everything.
- **"Generic 'View All' anchors"** (second review): already contextual
  ("View All Panel Machines", "View All Gutter Machines", …).
- **Hero LCP attributes**: slide 1 already ships `loading="eager"` +
  `fetchpriority="high"`, later slides lazy. Correct as-is.
- **417KB HTML page weight / CrUX monitoring**: real but out of scope for a
  homepage content pass; CrUX visibility requires configuring `CRUX_API_KEY`
  in the reporting worker (infrastructure task, not a theme change).
